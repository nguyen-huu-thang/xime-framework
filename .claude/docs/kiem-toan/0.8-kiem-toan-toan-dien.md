# Kiểm toán toàn diện 0.8.0 - ĐỢT 1

> Đọc từng dòng phần mã **mới** của mô hình đa tiến trình. Khác với lượt rà
> [`0.8-truoc-phat-hanh.md`](0.8-truoc-phat-hanh.md) (2026-08-20): lượt đó soi
> **đóng gói, tài liệu, phiên bản**; lượt này soi **cơ chế**.
>
> Trạng thái: **ĐỢT 1 XONG, chưa vá gì.** Danh sách ra trước, chủ dự án duyệt rồi
> mới vá, để phần vá có đối chứng riêng.

## Phạm vi đã đọc

| Mảng | Dòng | Mức |
|---|---|---|
| `core/link/` (bus) | 1.724 | **đọc từng dòng** |
| `core/refdata/` | 1.238 | **đọc từng dòng** |
| `core/bootstrap/` phần mới (`_supervisor` `_processes` `_slot` `_shared` `_watchdog` `_cluster` `_control` `_sdnotify` `_health`) | ~3.100 | **đọc từng dòng** |
| `starters/lmdb/` | 1.406 | `_env` `_store` `_config` đọc từng dòng; `_cleanup` `_storage` soi ngang |
| `adapters/web/_health.py` | 130 | đọc từng dòng - kéo vào vì nó quyết mức độ của T5 |

⚠ **Chưa đọc, thuộc đợt 2:** `application.py` (1.011 dòng), `orchestrator.py`,
`cli/` phần mới, `_startup.py`, và 122 file bị sửa vì đổi API adapter.

## Tóm tắt

| Mức | Số | Ghi chú |
|---|---|---|
| **Cao** | 2 | cả hai là **quyền file trên Linux**, và cả hai chỉ đo thật được trên Linux |
| **Trung** | 7 | |
| **Thấp** | 5 | |
| Đã kiểm, sạch | 6 | ghi lại để lượt sau khỏi đo lại |

⭐ **Điều đáng nhớ nhất của đợt này:** năm trong mười bốn phát hiện có cùng một hình
dạng - **một cơ chế phòng thủ có mặt nhưng không bao giờ chạy**. Phép dò đo sai đại
lượng, hàm dọn rác không ai gọi, cờ chẩn đoán chỉ nhìn thấy được từ tiến trình đã
hỏng, chú thích mô tả một việc chưa hiện thực.

Đây đúng khuôn repo này đã tự vấp hai lần ngay trong lúc thi công 0.8
(`@runtime_checkable` chưa từng có tác dụng vì Protocol chỉ import dưới
`TYPE_CHECKING`; `_is_main_guard` là mã chết vì nó soi sai tầng AST) và một lần ở
workspace (`ShardValueGuard` đếm bảng rỗng là đã kiểm chứng).

> **Một chốt chặn không chạy trông y hệt một chốt chặn không có việc để làm.**

---

# 🔴 CAO

## C1 - File kho LMDB để quyền mặc định của hệ điều hành, trong khi nó giữ hãm nhịp và thử thách passkey

**Ở đâu:** `xime/starters/lmdb/_env.py:169`

```python
env = lmdb.open(
    file_path,
    subdir=False,
    map_size=size,
    sync=False,
    metasync=False,
    max_dbs=0,
    lock=True,
)          # <- KHÔNG có mode=
```

`py-lmdb` mặc định `mode=0o755`, và với `subdir=False` thì file dữ liệu cùng file
khoá được tạo theo `mode & ~umask`. umask thường gặp trên server là `022`, ra
**0644 / 0755, tức mọi user trên máy đọc được**. Thư mục cha cũng vậy:
`Path(file_path).parent.mkdir(parents=True)` dùng mặc định `0o777 & ~umask`.

**Nội dung của kho này là gì:** theo đúng thiết kế kho nhóm 2 - **hãm nhịp đăng
nhập, thử thách passkey, chống lặp**. Đọc được bảng hãm nhịp là biết mình còn bao
nhiêu lượt thử và khi nào cửa sổ mở lại; đọc được thử thách passkey là chạm vào vật
liệu của một luồng xác thực đang diễn ra.

**Vì sao xếp Cao chứ không phải Trung:** chính repo này đã ra quyết định ngược lại
cho cùng loại dữ liệu, và ghi lý do vào code.
`xime/starters/localfs/_storage.py:94`:

```python
# Objects are customer data. The process umask usually produces 0644,
# ...
self._file_mode: int = _parse_mode(config.get("storage.local.file_mode"), 0o600)
self._dir_mode:  int = _parse_mode(config.get("storage.local.dir_mode"),  0o700)
```

Chuẩn đã có trong nhà từ 0.7 (phát hiện F13), và mảng mới không đi theo. Đây không
phải một câu hỏi thiết kế còn mở; nó là một chỗ bỏ sót.

**Đề xuất:** thêm `mode=` vào `lmdb.open` với mặc định `0o600`, tạo thư mục rồi
`os.chmod(..., 0o700)`, và cho người vận hành khai được qua `lmdb.file_mode` /
`lmdb.dir_mode` đúng khuôn `storage.local.*`.
⚠ `mkdir(mode=)` bị umask cắt nên phải `chmod` lại sau khi tạo, đừng chỉ truyền
tham số.

**Đo:** ⏳ chưa kết luận được trên Windows - quyền POSIX không có nghĩa ở đây.
Xem mục **L-01** của [sổ bàn giao Linux](0.8-cho-do-tren-linux.md).

---

## C2 - Cha bind unix socket rồi `listen()` mà KHÔNG chmod, nên nó mở toang suốt cả lượt khởi động của con

**Ở đâu:** `xime/core/bootstrap/_supervisor.py:400-424` (`_bind_unix`)

```python
sock = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
sock.bind(spec.path)      # <- quyền = mặc định umask
sock.listen(_BACKLOG)     # <- TỪ ĐÂY ĐÃ NHẬN KẾT NỐI
sock.set_inheritable(True)
```

`grep -c chmod xime/core/bootstrap/_supervisor.py` ra **0**.

**Vì sao đây là lỗ chứ không phải chuyện nhỏ.** Adapter socket dựng cả mô hình bảo
mật của nó trên quyền file, và mặc định để quyền file **một mình** gánh việc đó:

| | |
|---|---|
| `SocketServerConfig.permission` | `0o600` (`socket/_config.py:37`) |
| `allowed_uids` | `()` mặc định |
| `authorize_peer` nói gì về mặc định đó | *"Whitelist rỗng = chấp nhận mọi UID; lúc đó dựa hoàn toàn vào file permission"* |
| Docstring adapter | *"Bảo mật dựa trên kernel Linux: file permission (chmod/chown) + SO_PEERCRED"* |

Con **có** chmod: `secure_socket_file()` được gọi vô điều kiện ở
`socket/_adapter.py:163`, kể cả nhánh kế thừa socket. Nên đây **không phải một lỗ
vĩnh viễn, mà là một CỬA SỔ**. Vấn đề nằm ở độ dài của cửa sổ đó:

```text
cha _bind_unix() + listen()        <- socket đã nhận kết nối, quyền theo umask
  -> allocate_shared_memory
  -> spawn primary                 <- con import lại main.py (đo: 721 module, ~1s)
  -> con dựng DI, post_construct   <- mở pool DB, lấy cert
  -> con chạy run_once()           <- MIGRATION
  -> ... adapter socket start()
  -> secure_socket_file()          <- chmod 0600, cửa sổ đóng
```

Chính framework khai rằng cửa sổ này có thể dài **tới 60 giây**:
`_RUN_ONCE_WAIT = 60.0` và `STARTUP_GRACE_SECONDS = 60.0` tồn tại vì lượt khởi động
được cho là có thể tốn ngần đó.

Kết nối đến trong cửa sổ đó không bị từ chối. Chúng nằm trong backlog 2048 mà cha đã
`listen()`, rồi được con phục vụ ngay khi nó sẵn sàng.

**Đây là hồi quy do 0.8 sinh ra.** Ở nhánh một tiến trình,
`start_unix_server(path=...)` và `secure_socket_file()` là hai dòng liền nhau, cửa
sổ cỡ micro giây. 0.8 tách **người bind** (cha) khỏi **người chmod** (con), và kéo
cửa sổ đó ra hàng chục giây.

**Đề xuất:** `os.chmod(spec.path, 0o600)` ngay sau `bind()` và **trước** `listen()`.
Cha không biết giá trị `permission` đã cấu hình (nó nằm ở `socket.<id>.permission`,
mà cha không dựng DI) - nhưng thứ tự đúng là **chặt trước, nới sau**: cha khoá
0600, con nới ra giá trị đã khai nếu nó rộng hơn. Chiều ngược lại mới là chiều nguy.

**Đo:** ⏳ chưa kết luận được trên Windows. Xem mục **L-02** của
[sổ bàn giao Linux](0.8-cho-do-tren-linux.md).

---

# 🟠 TRUNG

## T1 - Watchdog dùng ĐỒNG HỒ TƯỜNG, nên một bước nhảy NTP về phía trước giết sạch con đang khoẻ

**Ở đâu:** `_watchdog.py:186` (`pat()` ghi `time.time()`) và `_supervisor.py:944`
(`_reap_hung_children` đọc `now = time.time()`).

**Đo được trên máy này:**

```text
vua vo xong  : silent_for = 0.00s   -> giet? False
dong ho +30s : silent_for = 30.00s  -> giet? True      <--
dong ho -30s : silent_for = -30.00s -> giet? False
```

Một con vừa vỗ nhịp xong, hoàn toàn khoẻ, bị xếp là *"event loop bị chặn"* và bị
`proc.kill()`, chỉ vì đồng hồ tường nhảy lên.

Và nó nhảy lên **cùng lúc cho mọi con**, nên nó không giết một con - nó giết **cả
cụm**. Sau đó cụm dựng lại, `_promote_someone` đếm đủ 3 lần thăng cấp trong 60
giây và **dừng cấp vai primary vĩnh viễn**. Từ đó không job nền nào chạy nữa, và
dòng log giải thích chuyện đó là:

> *"this looks like a job that kills whoever runs it"*

tức nó dẫn người vận hành đi tìm đúng chỗ không có gì sai.

Bước nhảy tiến không hiếm: VM khôi phục từ suspend, lần đồng bộ NTP đầu tiên sau
khi RTC sai, `chronyd` bước thay vì trượt khi lệch quá ngưỡng.

⭐ **Chỗ làm nó thành phát hiện chắc chắn chứ không phải lo xa: hai nhánh của cùng
một hàm dùng hai đồng hồ khác nhau.**

```python
silent = beats.silent_for(index, now=now)            # now = time.time()  <- tường
...
age = time.monotonic() - self._spawned_at.get(...)   #                    <- đơn điệu
```

Nhánh ân hạn khởi động đã miễn nhiễm; nhánh im lặng thì không.

Và lập luận *"monotonic không so được giữa các tiến trình"* không phải lý do của
lựa chọn này, vì phần còn lại của 0.8 làm ngược lại: `core/link` và `core/refdata`
**đã** so `time.monotonic_ns()` giữa các tiến trình (`ROW_WRITTEN_AT` trong
`stats()` và `dump()`).

**Đề xuất:** đổi `pat()` và `_reap_hung_children` sang `time.monotonic()`. Kèm test
canh: giả lập đồng hồ nhảy +30s thì **không con nào bị giết**.

## T2 - Cảnh báo "kênh sắp đầy" của bus ĐO SAI ĐẠI LƯỢNG, nên nó không kêu ở đúng ca nó sinh ra để bắt

**Ở đâu:** `core/link/_link.py:728-745` (`_maybe_warn_full`), gọi từ `_claim_row`,
tức trên **đường ghi**.

```python
unread = len(layout.unread_rows(view, self._index))   # <- hộp thư ĐẾN của tôi
ratio = unread / layout.total_rows                    # <- chia cho TỔNG cả kênh
```

Nó đo *"bao nhiêu tin gửi TỚI tôi mà tôi chưa đọc"*, rồi in ra một câu nói về
*"bảng sắp đầy, hãy tìm một tiến trình đang tắc"*. Đại lượng đúng phải là *"vùng
ghi CỦA TÔI còn bao nhiêu dòng người khác chưa đọc"*.

**Đo được:** hai tiến trình, mỗi người 8 dòng, tiến trình 0 chỉ gửi và không nhận gì
- đúng hình dạng của cha nói với con qua kênh `__xime__`:

```text
vung ghi cua toi : 8/8 dong con nguoi khac chua doc     <- 100% ĐẦY
phep do dang dung: 0/16                                 <- 0%, khong bao gio kêu
```

Cảnh báo 80% tồn tại để báo **trước khi mất tin**. Ở ca này nó im, và thứ duy nhất
kêu là `_warn_missed` - thứ chỉ kêu **sau khi tin đã mất rồi**.

Sai theo cả chiều ngược lại: một tiến trình chậm đọc hộp thư đến sẽ kêu *"kênh sắp
đầy"* trong khi vùng ghi của nó rỗng không. Cả hai chiều đều là thứ khiến một cảnh
báo bị học cách lờ đi.

**Không có một test nào** chạm tới cảnh báo này - `grep` trong `tests_temp/link/` ra
rỗng. Đó là lý do nó sống.

**Đề xuất:** đo trên `layout.rows_of(self._index)`, đếm dòng còn `any_unread`, chia
cho `rows_per_writer`. Kèm test đi **thành cặp**: người chỉ gửi thì phải kêu, người
chỉ nhận thì phải im.

## T3 - `sweep_orphans()` không được gọi ở bất cứ đâu, trong khi nó được khai là một trong ba lớp dọn rác

**Ở đâu:** `core/link/_cleanup.py`.

Docstring của chính nó dựng một bảng ba lớp và xếp mình vào lớp thứ hai:

| Lớp | Che gì |
|---|---|
| `unlink()` trong `finally` | tắt êm, `Ctrl+C`, `SIGTERM` - 99% số lần |
| **Hàm ở file này, chạy lúc khởi động** | **`kill -9`, mất điện** |
| `resource_tracker` của Python | có sẵn |

`CHANGELOG.md:1374` cũng quảng cáo nó. Nó nằm trong `__all__`. Nó có 4 test.

Nhưng:

```bash
grep -rn "sweep_orphans" --include=*.py . | grep -v __pycache__
# _cleanup.py (định nghĩa) - __init__.py (export) - test_cleanup.py (4 test)
```

**Không đường khởi động nào gọi nó.** Lớp che `kill -9` và mất điện là lớp duy nhất
không có gì thay thế, và nó không chạy. Trên Linux `/dev/shm` là RAM, nên rác ở đó
là RAM không ai đòi lại.

⚠ Cùng khuôn hai lỗi đã tự bắt được lúc thi công: *phép kiểm đúng, chỗ dùng nó
không tồn tại*. Ba lần trong một bản.

**Đề xuất:** gọi trong `run_supervisor()` trước `allocate_shared_memory`, và ở nhánh
một tiến trình trước `ProcessLink.create`. Kèm test canh **chỗ gọi**, không chỉ test
bản thân hàm - đó chính là chỗ hai lần trước bị hở.

## T4 - Vùng nhớ của `RefData` KHÔNG BAO GIỜ được dọn, và một chú thích nói ngược lại

**Ở đâu:** `core/refdata/_arena.py:50`

```python
# Tiền tố tên vùng nhớ. Cùng khuôn với bus (`xime-link-...`) để một lần dọn rác
# nhìn thấy cả hai họ.
_PREFIX = "xime-ref"
```

Nhưng `sweep_orphans` lọc bằng:

```python
_PREFIX = "xime-link-"
if not entry.name.startswith(_PREFIX):
    continue
```

`xime-ref-...` **không khớp**. Câu chú thích mô tả một ý định chưa được hiện thực,
và đó là loại chú thích tệ nhất: nó khiến người đọc sau tin rằng việc đã xong nên
không ai đi kiểm.

Bảng nhịp watchdog (`xime-beat-...`) cũng nằm ngoài mọi phép dọn.

Cộng với T3 thì kho tham chiếu thiếu **hai** lớp chứ không phải một: không ai quét
nó, và cái quét duy nhất tồn tại thì cũng không nhìn thấy nó.

**Đề xuất:** cho `sweep_orphans` nhận cả ba tiền tố, rồi sửa chú thích cho khớp với
code.

## T5 - `stats().stale` của `RefData` chỉ nhìn thấy được từ chính tiến trình đã hỏng

**Ở đâu:** `core/refdata/_refdata.py` - `self._stale` và `self._warned_full` là
thuộc tính **instance**, sống trong RAM của tiến trình gọi `publish()`, tức primary.

Thiết kế khai ba lớp bảo vệ cho ca *"bản mới không vừa trần"*:

| Lớp | Sống ở đâu |
|---|---|
| cảnh báo 80% (`_warned_full`) | **RAM của primary** |
| nổ nhưng giữ bản cũ | ném về người gọi, tức primary |
| cờ `stale` trong `stats()` | **RAM của primary** |

Mà đây là ca mà chính docstring gọi là *"chỗ tệ nhất của cả cơ chế này"*: cụm chạy
êm bằng bản cũ, **không request nào lỗi**, cho tới lúc có thứ phụ thuộc vào bản mới
xuất hiện.

Từ bất kỳ tiến trình nào khác - kể cả tiến trình đang phục vụ `/healthz` -
`stats().stale` **luôn là `False`**. Ba lớp phòng thủ thu về một, và lớp còn lại
nằm đúng ở chỗ người vận hành không nhìn.

⚠ Kèm một hệ quả kiểu luật 03: ngày nào `/healthz` chở `stale` thì **cùng một đường
dẫn trả hai câu trả lời khác nhau tuỳ con nào nhận request**.

**Đề xuất:** đưa cờ vào **header vùng nhớ chung** thay vì để trong RAM. Header hiện
có 2 byte đệm ở offset 6 chưa dùng tới.

⭐ Làm bây giờ rẻ, làm sau là **đổi khuôn vùng nhớ** - đúng lập luận thiết kế đã
dùng để đưa `so_doan` vào từ v1 (*"hai byte bây giờ rẻ hơn một lần đổi khuôn về
sau"*), chỉ là lần này áp cho `stale`.

## T6 - `_check_parts` xoá cả một thư mục bảng, và N tiến trình cùng chạy nó

**Ở đâu:** `starters/lmdb/_env.py` (`_check_parts`)

```python
shutil.rmtree(table_dir, ignore_errors=True)
table_dir.mkdir(parents=True, exist_ok=True)
marker.write_text(str(parts), encoding="utf-8")
```

Chạy ở lần đầu mở mỗi bảng, bên trong `env_for`. Dưới `share_load()` thì **mọi tiến
trình đều mở cùng bảng đó**, nên sau một lần đổi `parts` trong code, cả N tiến trình
cùng thấy marker lệch và cùng chạy khối này. Đan xen được:

```text
A: rmtree(table_dir)
B: rmtree(table_dir)          <- xoá luôn thư mục A vừa tạo
A: mkdir + open 0.mdb
B: mkdir + write marker       <- A đang giữ fd trỏ vào một inode đã mất tên
```

`ignore_errors=True` nuốt mọi va chạm, nên nó **không có triệu chứng**: A ghi vào
một file không còn tên, và triệu chứng cuối cùng đúng bằng thứ mà chính docstring
nói là không ai lần ngược được - *"`get()` trả None nhiều hơn bình thường, trông y
hệt một cache lạnh"*.

Và **đổi `parts` là một sự kiện lúc triển khai**, tức đúng lúc N tiến trình cùng
khởi động lại.

**Đề xuất:** chỉ primary được chạy `_check_parts`, và chạy trong `run_once()`. Cơ
chế cần thiết đã có sẵn: cha đợi `run_once` xong mới sinh con thứ hai. Con khác chỉ
**đọc** marker, lệch thì nổ chứ không tự xoá.

## T7 - Con không khởi động được thì cha dựng lại mỗi giây, mãi mãi, không có hãm

**Ở đâu:** `_supervisor.py:1057` - `time.sleep(_RESPAWN_DELAY)` với
`_RESPAWN_DELAY = 1.0` cố định.

Chống domino chỉ áp cho **thăng cấp**, và code nói rõ là cố ý:

> *"cha VẪN dựng lại con đã chết, chỉ thôi cấp vai primary"*

Quyết định đó đúng, và không đề nghị đổi. Nhưng *luôn dựng lại* khác với *luôn dựng
lại ở nhịp cố định một giây*. Một con chết ngay lúc khởi động (cấu hình sai, thiếu
thư viện, cổng bị chiếm) cho ra một vòng lặp:

| Mỗi giây | |
|---|---|
| một `spawn` | import lại toàn bộ cây module - đo được **83 MB RSS, 721 module, ~1s** |
| một dòng `WARNING` | |
| vô hạn | không ngưỡng, không leo thang, không ai được báo |

Thực tế là **một nhân bị đốt trọn cho tới khi có người nhìn vào**. Và vì
`time.sleep` nằm trong vòng `_watch`, cha còn thưa nhịp `sd_notify` trong lúc đó.

**Đề xuất:** hãm luỹ tiến có trần (1s, 2s, 4s... tối đa 30s), đặt lại về 1s khi một
con sống quá một ngưỡng. Giữ nguyên lời hứa *"luôn dựng lại"*. Kèm leo thang log:
từ lần thứ 10 trở đi thì `CRITICAL` chứ không `WARNING`.

---

# ⚪ THẤP

## L1 - `read_payload` không ép trần độ dài, nên một dòng hỏng đọc lan sang vùng của người khác

`core/link/_layout.py` - `read_payload` dùng thẳng `read_length(...)` làm độ dài lát
cắt, không kẹp về `payload_bytes`.

**Đo được:** kênh khai `payload_bytes=64`, bóp méo trường độ dài rồi đọc lại ra
**2.104 byte** - vượt biên dòng, sang các dòng khác và sang vùng ghi của tiến trình
khác.

Không phải lỗi bộ nhớ (memoryview tự cắt ở cuối block), nhưng nó biến một dòng
thành một bản trích của cả kênh, và giá trị đó chảy tiếp vào `stats()` và `dump()`.
Sửa bằng một `min(...)`.

## L2 - `ProcessLink.create` kiểm `index` SAU khi đã cấp vùng nhớ

`_link.py:144-178`: khối `try/except BaseException` chỉ bọc vòng tạo block; phép
kiểm `0 <= index < process_count` nằm **sau** cả block lẫn semaphore. Ném ở đó thì
vùng nhớ không được `unlink`.

Đo trên Windows: **tự dọn** - vùng nhớ biến mất khi handle cuối đóng. Trên Linux thì
không: ⏳ mục **L-03** của sổ bàn giao. Sửa bằng cách chuyển phép kiểm lên đầu hàm.

## L3 - `_parse_endpoint` không kiểm dải cổng

`_processes.py` kiểm `port` phải là `int` nhưng không kiểm `0 <= port <= 65535`.
`port: 99999` đi qua mọi phép kiểm cấu hình rồi nổ ở `bind()` bằng một `OSError`
thô. Một khối cấu hình có cả một tầng thông báo lỗi viết cẩn thận thì chỗ này nên
nói cùng một giọng.

## L4 - `ask()` lúc hết giờ đọc `ROW_TAKER` của một dòng có thể đã bị tái dùng

`_link.py:341-352`: nhánh `TimeoutError` đọc lại byte `nguoi_nhan` trên `row` đã gửi
để tách `NoAnswer` khỏi `NoOwner`. Nhưng con trỏ ghi vòng lại sau `rows_per_writer`
tin (mặc định 256), nên dưới tải một `ask` chờ lâu có thể đọc byte của **một tin
khác**.

Hậu quả là xếp nhầm giữa *"lỗi cấu hình, đừng thử lại"* và *"tiến trình kia chậm"* -
đúng hai việc khác nhau mà bốn kết cục được dựng lên để tách. Sửa bằng cách nhớ
`sequence` lúc gửi và so lại trước khi tin byte đó.

## L5 - `bump_missed` là đọc-sửa-ghi không nguyên tử, và không được khai như `next_sequence`

`_layout.py`: `next_sequence` có một đoạn chú thích dài giải thích vì sao mất mát ở
đó chấp nhận được. `bump_missed` cùng hình dạng nhưng **không có dòng nào** - trong
khi `missed` được chính thiết kế gọi là *"chỉ số chẩn đoán chính của bus"*.

Hai người ghi cùng đè dòng của cùng một người đọc thì một lần đếm biến mất, và con
số người vận hành nhìn thấy nhỏ hơn sự thật. Không cần sửa cơ chế; cần một dòng
khai, để người sau không đọc con số đó chặt hơn thứ nó hứa.

---

# ✅ Đã kiểm, SẠCH - đừng làm lại

| Nghi vấn | Kết quả |
|---|---|
| **Hai `publish()` đồng thời trong cùng primary có làm rách bản không** | **Không.** Chạy 5 lượt: `so doi` đi 1 -> 3, nội dung nguyên vẹn, không lẫn hai lần ghi. Lý do: `_write` **không có `await` nào bên trong**, nên dưới asyncio nó nguyên tử với event loop. ⚠ Kết luận gắn với điều kiện đó - ai thêm một `await` vào `_write` là mở lại câu hỏi này |
| **Tên vùng nhớ chung có đoán được không** | **Không.** `f"{os.getpid()}-{secrets.token_hex(8)}"`, và `SharedMemory(create=True)` dùng `O_CREX` nên không chiếm chỗ trước được |
| **Tên kênh / tên bảng / id tiến trình có traversal không** | **Không.** `^[A-Za-z0-9][A-Za-z0-9._-]*$` chặn `/` và chặn tên bắt đầu bằng `.`, nên `shutil.rmtree(table_dir)` của LMDB không ra ngoài `lmdb.path` được |
| **`/healthz` có lộ gì không** | **Không.** Thân phản hồi chỉ có `alive` `ready` `primary` và danh sách `{id, kind, state}`. Không host, không cổng, không phiên bản, không thông điệp lỗi. Việc nó không đòi token là **quyết định đã ghi lý do**, không phải sót |
| **Server phụ có âm thầm rơi về HTTP thuần không** | **Không.** `WebAdapter.resolve_tls` kế thừa `server.ssl` khi ô không khai; muốn HTTP thuần phải viết `ssl: {}` tường minh |
| **Cha dọn socket mồ côi có cướp chỗ con đang sống không** | **Không.** Cha dọn **một lần trước khi có con nào**, và con khai `_owns_socket_file = slot is None or slot.sock is None` nên con kế thừa socket không xoá file lúc tắt |

---

# Việc kế tiếp

1. **Chủ dự án duyệt danh sách này**, rồi mới vá. Mỗi bản vá kèm đối chứng: gỡ ra,
   đếm test đỏ.
2. C1, C2 và L2 **chưa kết luận được trên Windows** - xem
   [`0.8-cho-do-tren-linux.md`](0.8-cho-do-tren-linux.md).
3. Đợt 2: `application.py`, `orchestrator.py`, `cli/` phần mới, `_startup.py`, và
   122 file bị sửa vì đổi API adapter.

---
---

# ĐỢT 2 - XONG (2026-08-21)

> Ghi dần trong lúc đọc chứ không đợi tới cuối, theo yêu cầu chủ dự án.
>
> **4 phát hiện mới: 1 Cao, 3 Trung.** Cộng 5 nghi vấn đã kiểm và sạch, và bốn bộ
> test thật chạy lại không có hồi quy.

## Phạm vi đợt 2

`application.py` (1.011) · `orchestrator.py` (295) · `cli/` phần mới (1.681) ·
`_startup.py` · 122 file bị sửa vì đổi API adapter · bề mặt `/healthz`.

# 🔴 CAO

## C3 - Thăng cấp primary KHÔNG chạm tới `RefDataArena`, nên primary mới không publish được nữa

**Ở đâu:** `application.py:695` (`_accept_promotion`) và `refdata/_arena.py:118`.

Cờ *"tôi có phải primary không"* tồn tại ở **hai chỗ**, và chỉ một chỗ được cập nhật
khi cha trao vai:

| Nơi giữ cờ | Đặt lúc nào | Có đổi khi thăng cấp không |
|---|---|---|
| `Application._is_primary` | lúc khởi động, từ `SharedHandle.primary` | ✅ `_accept_promotion` đặt `True` |
| `RefDataArena._primary` | lúc `attach()`/`create()`, từ cùng giá trị đó | ⛔ **không**, và **không có setter nào tồn tại** |

`grep -n "_primary" xime/core/refdata/_arena.py` ra đúng hai dòng: gán trong
`__init__` (118) và `return` trong property (211). Không ai gán lại.

Mà `RefData.publish()` hỏi **đúng cái cờ không được cập nhật**
(`_refdata.py:335`):

```python
if not self._arena.primary:
    raise RefDataNotWriterError(...)
```

**Đo được:**

```text
truoc thang cap: arena.primary = False
  publish bi chan dung nhu thiet ke: RefDataNotWriterError

sau thang cap : Application._is_primary = True
                arena.primary            = False
  -> publish VAN BI CHAN: RefDataNotWriterError
     "publish() was called from a non-primary process..."
```

**Vì sao xếp Cao.** `RefData` sinh ra cho **khoá JWT lấy từ Trust** - đó là ca dùng
số một trong chính tài liệu thiết kế, và là lý do `publish()` bị giới hạn cho
primary. Chuỗi hỏng:

```text
primary chết  ->  cha thăng cấp con B  ->  B nhận vai, báo PROMOTED
                                        ->  /healthz của B trả primary: true
                                        ->  job xoay khoá của B gọi publish()
                                        ->  RefDataNotWriterError, MÃI MÃI
                                        ->  khoá JWT không bao giờ được cập nhật nữa
                                        ->  ngày Trust xoay khoá: mọi token mang kid
                                            mới verify hỏng ở TOÀN CỤM
```

⚠ **Và triệu chứng nói ngược với sự thật.** Mọi tín hiệu khác đều nói B là primary:
log của cha (*"took the primary role"*), `PROMOTED` trên kênh điều khiển,
`/healthz` trả `"primary": true`. Chỉ một dòng lỗi nói *"called from a non-primary
process"*. Người đọc sẽ đi tìm lỗi ở cơ chế thăng cấp, không tìm ở cờ.

⭐ Đây là **luật 03 ở tầng trạng thái**: một câu hỏi (*"tiến trình này có phải
primary không"*) có hai nguồn trả lời, và chúng lệch nhau ngay ở tình huống mà cả
cơ chế thăng cấp được dựng lên để phục vụ.

**Không test nào bắt được** vì bộ test thăng cấp đo *"adapter đơn nhất có khởi động
không"* và *"có đúng một người nhận vai không"* - không phép đo nào chạm tới
`RefData` sau thăng cấp.

**Đề xuất:** một nguồn sự thật, không phải hai. Hoặc `RefDataArena` đọc cờ từ
`Application` qua một callable, hoặc `_accept_promotion` gọi
`self._refdata.promote()` và `RefDataArena` có setter (và cả `_is_primary = False`
ở nhánh từ chối vai phải gọi ngược lại). ⚠ Nhánh **từ chối vai** cũng phải quay
cờ về, nếu không thì một con từ chối vai lại publish được.

Test canh: thăng cấp xong thì `publish()` **chạy**; từ chối vai xong thì `publish()`
**bị chặn**. Đi thành cặp, vì sửa sai một chiều (*"cho publish luôn"*) cũng qua được
test một vế.

---

# 🟠 TRUNG (đợt 2)

## T8 - `xime init config` sinh ra một dự án KHÔNG khởi động được, và trình tạo không nói gì

**Ở đâu:** `xime/cli/_init.py` (`build_plan`) và `validate_name`.

`validate_name` chấp mọi tên khớp `^[a-z][a-z0-9-]{0,63}$`. `build_plan` dựng một
**dict** trong đó cả framework lẫn dự án cùng đặt file:

```python
files = {
    "config/__init__.py":        CONFIG_INIT,        # <- khai `dependency`
    ...
    f"{module}/__init__.py":     f'"""{project}."""\n',   # <- ghi đè khi module == "config"
    ...
}
```

Tên dự án `config` cho `module == "config"`, nên **hai khoá trùng nhau** và khoá
sau thắng.

**Đo được:**

```text
config       -> module 'config', 11 file   <<< VA TEN
                config/__init__.py = '"""config."""'
                -> co 'dependency' khong? False
don-hang     -> module 'don_hang', 12 file
```

**11 file thay vì 12**, và `config/__init__.py` mất hẳn dòng
`from config.dependency import dependency`. `app.add_config(config)` sau đó không
tìm thấy `dependency`, tức dự án chết ngay lần chạy đầu - bằng một thông báo nói về
`config`, không nói gì về việc tên dự án là thủ phạm.

**Hai tên nữa cùng họ, cùng cơ chế nhưng khác kiểu hỏng:**

| Tên | Chuyện gì |
|---|---|
| `xime init xime` | sinh một package `xime/` ngay cạnh `main.py`. `_main.py:404` chèn cwd vào `sys.path`, và chạy `python main.py` cũng đặt thư mục script lên đầu - nên `import xime` **lấy package rỗng vừa sinh** thay vì framework |
| `xime init json` (hay bất kỳ tên module stdlib nào) | khuôn shadow kinh điển: `import json` ở bất kỳ đâu trong dự án lấy package của dự án |

**Đề xuất:** `validate_name` từ chối một danh sách ngắn - `config`, `resources`,
`xime`, `main`, `test`, `tests` - cộng `sys.stdlib_module_names`. Thông báo nói
thẳng vì sao, và gợi ý một tên khác. Rẻ, và nó chặn đúng lúc người ta còn đang gõ
lệnh chứ không phải lúc dự án đã có code.

⚠ Kèm một chỗ đáng sửa trong `build_plan` dù đã chặn ở trên: dựng bằng **dict
literal** khiến một va tên biến thành *ghi đè im lặng*. Ném khi phát hiện khoá
trùng thì lỗi nói đúng bệnh, và nó bắt được cả những tên chưa ai nghĩ tới.

---

# ✅ Đã kiểm, SẠCH (đợt 2)

| Nghi vấn | Kết quả |
|---|---|
| **`/healthz` có thật sự đi qua được middleware JWT không** | **Có.** `_adapter.py:520-527` lấy `public_health_paths(server_id)` rồi **cộng vào `jwt_config.public_paths`** trước khi gắn middleware. Đây là chỗ một lượt đối chứng trước đã từng hở và đã vá; nay vá đúng |
| **`xime init` có ghi đè dự án đang chạy không** | **Không.** `_main.py:165-176` gọi `plan.existing()` **trước** khi ghi, in ra từng file va chạm, và chỉ đi tiếp với `--force`. Thông báo nói thẳng *"overwriting resources/application.yml would erase a real deployment's configuration"* |
| **Phép dò `warn_if_module_level_is_heavy` có bị bỏ quên như `sweep_orphans` không** | **Không.** Nối đúng ở `application.py:449` (đóng băng mốc) và `:472` (kêu), và bộ test có hẳn nhóm đo **chỗ gọi** chứ không chỉ đo hàm (`test_startup_cost.py:195-281`) |
| **Sáu adapter có đủ `start`/`serve`/`stop` sau khi tách đôi vòng đời không** | **Có, 6/6** cộng `SchedulerAdapter`. Không adapter nào còn gộp hai giai đoạn |
| **`_serve_adapters` có dùng `TaskGroup` không** | **Không**, và đó là quyết định đã ghi lý do: ngữ nghĩa *"một task lỗi thì anh em bị huỷ"* sai với `serve()`. Task của adapter được thăng cấp thêm vào sau vẫn bị `cancel()` đúng, vì `self._serving` **là chính** dict `tasks` |

## T9 - `xime check config` bắt được khoá gõ sai BÊN TRONG một khối, nhưng mù với tên KHỐI gõ sai

**Ở đâu:** `xime/cli/_config_check.py` (`check`)

```python
for name in seen:
    block = BY_NAME.get(name)
    if block is None:
        # Khối của chính ứng dụng. Framework không biết, và không đoán.
        continue
```

**Đo được:**

```text
'serber:'      -> verdict=clean,    findings=0,  checked=()
'server.porrt' -> verdict=findings, "unknown key: did you mean 'port'?"
```

Cùng một lỗi gõ, cùng một hậu quả, hai kết quả khác nhau tuỳ nó nằm ở tầng nào.

**Vì sao hậu quả giống nhau, và vì sao nó vừa nặng thêm.** Lượt rà 2026-08-20 vá
việc *"vắng `server:` thì rơi về cổng mặc định 8080"* để dự án do `xime init` sinh
ra chạy được. Bản vá đó đúng, nhưng nó khiến `serber:` thành:

> ứng dụng nghe ở **8080**, người vận hành tin là **8086**, `xime check config`
> nói **clean**, và không có một dòng log nào.

Đúng loại lỗi mà công cụ này được viết ra để bắt - docstring của nó mở đầu bằng
*"thứ nó bắt được mà hôm nay không gì bắt: khoá gõ sai"*.

**Vì sao bỏ qua tên khối lạ là quyết định ĐÚNG, và vì sao vẫn còn chỗ sửa.** Ứng
dụng có khối riêng (`trust:`, `app:`, `shard:`) mà framework không biết, nên tố mọi
tên lạ là kêu oan ngay ngày đầu. Nhưng có đường giữa: **gợi ý khi tên lạ GẦN GIỐNG
một tên đã biết**, im khi nó không giống gì cả.

Đo trên chính bộ tên khối của workspace này, `difflib` cutoff 0,7 tách sạch:

```text
serber   -> ['server']      sever  -> ['server']      grcp -> ['grpc']
trust    -> []              app    -> []              dental -> []      shard -> []
```

**0 cảnh báo giả** trên các khối thật của app, và bắt trọn ba kiểu gõ nhầm. Cùng
cơ chế `get_close_matches` mà hàm `_walk` **đã dùng** cho khoá con - chỉ là chưa
được áp ở tầng trên.

⚠ Ràng buộc kèm theo: phải sửa T10 trước, nếu không `process:` (một khoá **hợp
lệ**) sẽ bị gợi ý thành `processes` - biến bản vá thành đúng thứ nó tránh.

## T10 - Khối `process:` không có trong bản mô tả cấu hình, và phép dò trôi dạt mù về mặt cấu trúc với nó

**Ở đâu:** `xime/cli/_config_spec.py` (`BY_NAME`) và
`tests_temp/cli_config/test_spec.py`.

`BY_NAME` có 16 khối và **không có `process`**:

```text
cors database grpc jwt lmdb logging mail modbus mqtt
opcua processes redis server socket storage xime
```

Nhưng `_processes.py` khai `SINGLE_KEY = "process"` và `build_topology` đọc nó, và
lượt rà 08-20 chốt `process:` là **hình dạng chuẩn cho một tiến trình**. Hệ quả:
`xime config --print` không nói gì về nó, và `xime check config` bỏ qua trọn khối.

⭐ **Phần đáng giá hơn bản thân chỗ thiếu: vì sao phép dò trôi dạt không bắt được.**
`test_spec.py` quét AST tìm hai hình dạng:

| Hình dạng | Bắt được |
|---|---|
| `runtime.get("literal")` - `Attribute` trên một `Name` | ✅ |
| `FromConfig("literal")` | ✅ |
| **`read(SINGLE_KEY)`** - một `Name` trần, đối số là **hằng có tên** | ⛔ |

`build_topology(read, ...)` nhận `runtime.get` **làm tham số** rồi gọi
`read(SINGLE_KEY)`. Cả hai vế đều trượt: hàm không phải `x.get`, và đối số không
phải chuỗi literal.

Chính test đó **đã khai chỗ mù này** ở dòng 57 - *"(`runtime.get(name)`) thì không
tên nào để đọc"*. Nó khai đúng, rồi không ai đi kiểm chỗ mù đó có đang giấu cái gì
không. Nó đang giấu một khối.

📌 Cùng họ với ba ca ở đợt 1, nhưng ở dạng khó hơn: đây **không phải** một phép dò
không được gọi - nó chạy, nó xanh, và nó xanh **một cách chính đáng theo đúng thứ
nó tự khai là làm được**. Chỗ hỏng nằm ở khoảng giữa lời khai và kỳ vọng của người
đọc kết quả.

**Đề xuất:** thêm khối `process` vào `BY_NAME`, và cho phép dò trôi dạt nhận thêm
hình dạng *"đối số là một hằng ở mức module trong cùng file"* - `SINGLE_KEY` và
`MULTI_KEY` đều là `Final[str]` nên đọc được bằng AST không cần chạy code.

---

# ✅ Nghiệm thu đợt 2 - chạy thật

| Bộ test | Kết quả |
|---|---|
| Framework | **2412 passed, 14 skipped** (208s) - khớp đúng con số CLAUDE.md khai |
| `Base Platform/data` | **388 passed** |
| `Application Layer/linh-kien-dien-tu` | **295 passed** |
| `Service ngang/crm` | **53 passed** |

Không có hồi quy nào từ việc đổi API adapter trên bốn cây mã thật.

## ⚠ Một phép đo của chính tôi sai, ghi lại vì nó sẽ lặp

Lượt chạy `crm` đầu tiên cho **5 lỗi collection** trông y hệt một hồi quy:

```text
ModuleNotFoundError: No module named 'app.config.cong_tac'
ModuleNotFoundError: No module named 'app.infrastructure.persistence.data_map'
```

Cả hai module **đều tồn tại**. Tôi chạy `pytest` từ `crm/` trong khi package nằm
dưới `crm/backend/`. Chạy lại từ đúng chỗ: **53 passed**.

> Một `ModuleNotFoundError` trỏ vào package **của chính ứng dụng** thì gần như
> luôn là lỗi của người chạy lệnh, không phải lỗi của thư viện. Thư viện sai thì
> tên module sai sẽ mang tiền tố của thư viện.

Đây là lần thứ tư trong hai ngày mà công cụ đo nói dối trước khi mã nói dối. Giữ
thói quen: một kết quả bất ngờ thì nghi phép đo trước, nghi code sau.

---

# Việc kế tiếp sau đợt 2

Đợt 3 nằm ngay dưới.

---
---

# ĐỢT 3 - XONG (2026-08-21): chuỗi cung ứng, đóng gói, hợp đồng công khai

> **5 phát hiện mới: 3 Trung, 2 Thấp.** Cộng 8 phép đo chạy thật, tất cả sạch.
>
> ⭐ Đợt này khác hai đợt trước ở chỗ **phần lớn là tin tốt**: lớp đóng gói của 0.8
> lành. Ba mục Trung đều thuộc cùng một họ - **một cổng kiểm được khai trong tài
> liệu nhưng không có gì bảo đảm nó chạy**.

## T11 - `mypy` và `ruff` được CẤU HÌNH nhưng không được KHAI, nên cổng `mypy --strict` chưa hề chạy cho 0.8

**Ở đâu:** `pyproject.toml` có `[tool.ruff]` (dòng 370) và `[tool.mypy]` (dòng 406),
nhưng `grep "mypy\|ruff"` trong phần `dependencies` / `dev` ra **rỗng**. Đo trên máy
này: `import mypy` -> `ModuleNotFoundError`.

**Đây đúng là lỗi mà lượt rà 08-20 vừa vá - cho một công cụ khác.** Bản vá đó thêm
`pip-audit>=2.7` vào `dev` kèm một chú thích dài giải thích vì sao:

> *"Trước 2026-08-20 nó không được khai ở đâu cả - hướng dẫn phát hành phụ thuộc
> vào một chương trình mà không file nào nói là cần. Nó biến mất khỏi máy này lúc
> nào không ai biết, và script thì in "SACH" trong khi chưa hề soi được gì."*

Chẩn đoán đó đúng, và nó **đúng cho cả `mypy` lẫn `ruff`**. Bản vá chữa một **ca**
chứ không chữa một **loại**, nên hậu quả y hệt đã xảy ra ngay: `mypy` không có trên
máy, và cổng `mypy --strict` phía người dùng - một bước trong danh sách nghiệm thu
của kiểm toán 0.7.0 - **không chạy cho 0.8**.

⚠ Chính `pyproject.toml` giả định mypy là một phần của quy trình: chú thích của
`types-aiobotocore-s3` viết *"thiếu nó thì mypy trên chính `xime/` báo thiếu stub"*.
Một file khai một phụ thuộc **vì mypy**, trong khi không khai chính mypy.

**Đề xuất:** thêm `mypy>=1.x` và `ruff>=0.16` vào `dev`. Và, đáng giá hơn: chạy phép
kiểm ngược - **liệt kê mọi công cụ mà hướng dẫn phát hành hoặc `.claude/scripts/`
gọi tới, rồi đối chiếu với `dev`**. Đó là cách chữa loại thay vì chữa ca.

📌 `.claude/scripts/find_reexport_gap.py` tồn tại **chỉ để phục vụ** cổng mypy đó.
Nó chạy được (mã thoát 0) và in ra danh sách chỗ hở re-export, nhưng bản thân nó
không thay được mypy - nó chỉ chỉ chỗ đáng nhìn.

## T12 - Nợ luật 03 của `EventBus` hẹn trả ở 0.8, và 0.8 là CHUYẾN CUỐI

**Ở đâu:** `xime/core/event/bus.py:119-126`, còn nguyên trong mã 0.8.0:

```text
⚠ Bên gọi KHÔNG phân biệt được event bị bỏ với event đã xếp lịch - cả hai trả
None. Đây là nợ đã biết với luật 03, cố ý để lại cho 0.8 vì đóng nó là đổi chữ
ký công khai.
```

Nợ này được khai ở 0.7.2 với **một lý do cụ thể**: đóng nó đòi đổi chữ ký công
khai, nên phải đợi một bản cho phép phá tương thích. 0.8 là bản đó.

Nhưng `.claude/docs/lo-trinh-phien-ban.md` chốt:

| Dòng | Nội dung |
|---|---|
| 69 | *"**0.8** giữ `3 - Alpha`. ⭐ **Đây là bản Alpha cuối**"* |
| 70 | *"**0.8.x** giữ Alpha, và **không đổi API công khai một dòng nào**"* |
| 71 | *"**0.9** đổi `3 - Alpha` -> `4 - Beta`"*, nơi *"API coi như đã chốt"* |

> Nợ được xếp lên chuyến cuối, và chuyến cuối đang rời ga mà không chở nó.

Sau 0.8 chỉ còn hai đường, cả hai đều đắt hơn hôm nay: phá tương thích ở một bản
Beta, hoặc mang `publish()` trả `None`-hai-nghĩa vào 1.0 vĩnh viễn.

⚠ Và bản thân nợ này không nhỏ. Chính docstring khai hậu quả:

> *"Do not use the event bus for anything whose loss you must detect; drops are
> logged and counted, not reported back."*

Tức hợp đồng hiện tại buộc người dùng **nhớ một quy tắc** thay vì đọc một giá trị -
đúng thứ luật 03 mục 4 nói là sẽ mòn: *"người thêm RPC mới hai tháng nữa không đọc
lại lời cam kết đó"*.

**Đề xuất:** đây là một quyết định của chủ dự án, không phải một bản vá kỹ thuật.
Ba đường, xếp theo giá:

| Đường | Giá |
|---|---|
| `publish()` trả `bool` (hoặc một `Scheduled`/`Dropped`) | đổi chữ ký công khai - **chỉ làm được ở 0.8** |
| Thêm `try_publish()` bên cạnh, `publish()` giữ nguyên | thuần cộng thêm, làm được cả ở 0.8.x, nhưng để lại hai hàm làm một việc |
| Khai nhận và mang sang 1.0 | rẻ hôm nay, không đóng lại được sau đó |

Phiên này **không khuyến nghị đường nào** - nó chạm bề mặt công khai mà 31 app đang
dùng, thuộc nhóm *"quyết định cấu trúc thì hỏi chủ dự án trước"*.

## T13 - Dự án do `xime init` sinh ra nghe trên MỌI GIAO DIỆN MẠNG ngay lần chạy đầu, không có xác thực

**Đo được đầu-cuối** (venv trắng -> cài từ sdist -> `xime init` -> `python main.py`):

```text
web default: process main serving on 0.0.0.0:8080
```

`WebServerConfig.host` mặc định `"0.0.0.0"` (`_server_config.py:72,80`), và
`_FLAT_DEFAULT_PORTS["web"] = 8080` (khôi phục ở lượt rà 08-20). Dự án sinh ra để
`server:` ở dạng chú thích, nên nó nhận đúng hai mặc định đó.

Cùng lúc, `config/web.py` của dự án sinh ra **chỉ gọi `configure_controllers`** -
không `configure_jwt`, không `configure_cors`. Nên tổ hợp lần chạy đầu là:

> **Mọi giao diện mạng · cổng 8080 · không xác thực · không CORS.**

⚠ **Chỗ đáng nói không phải bản thân `0.0.0.0`** - đó là mặc định hợp lý cho một
framework chạy trong container, và uvicorn/gunicorn thì ngược lại (`127.0.0.1`).
Đây là một **lựa chọn**, và lựa chọn nào cũng có lý.

Chỗ đáng nói là **sự bất đối xứng trong chính file được sinh ra**:

```yaml
# Network binding for the HTTP adapter. An empty `ssl` block means
# plain HTTP. The certificate must come from a public CA (certbot):
# browsers do not trust an internal CA.
# server:
  # host: "0.0.0.0"
  # port: 8080
```

Ba dòng chú thích cẩn thận về TLS và CA công cộng, rồi in `host: "0.0.0.0"` **không
một chữ** nói rằng giá trị đó nghĩa là *"máy khác trong mạng gọi tới được"*. File
dạy thận trọng về cert, và im lặng về chỗ mở cửa.

📌 Và `8080` **va ngay trong phép đo này**: lần chạy đầu chết bằng `WinError 10048`
vì một thứ khác trên máy đã giữ cổng đó, mà thứ đó còn trả 404 cho `/ping` - tức
một lập trình viên có thể tưởng app mình đang chạy trong khi họ đang gọi vào chương
trình của người khác.

**Đề xuất** (rẻ, không đổi mặc định của framework): trình tạo ghi `server:` **mở
sẵn** với `host: 127.0.0.1` kèm một dòng chú thích nói thẳng *"đổi thành 0.0.0.0
khi chạy trong container"*. Dự án mới bắt đầu ở chỗ an toàn; người triển khai mở ra
khi họ biết mình đang làm gì. Mặc định của framework giữ nguyên cho 31 app hiện có.

---

# ⚪ THẤP (đợt 3)

## L6 - `check_dep_advisories.py` in một kết cục THỨ TƯ không nằm trong ba kết cục nó tự khai

Script khai ba kết cục (và CLAUDE.md chép lại): `SACH` (0) · `CHUA KET LUAN DUOC`
(2) · `CON MUC CHUA XU LY` (1). Nhưng nhánh *"có advisory, tất cả đều nằm trong
danh sách chấp nhận"* in:

```text
Khong con muc nao ngoai danh sach chap nhan.
```

và trả 0. Đó là một chuỗi thứ tư, không mang nhãn `SACH`. Người đọc log của bước 1b
đang tìm một trong ba từ khoá đã được dặn, và ở đường chạy **thường gặp nhất** họ
không thấy từ nào cả.

⚠ Kèm một chỗ hở nhỏ hơn: **mốc dương tính chỉ canh nhánh `not found`.** Nhánh
`found` đi thẳng tới phần phân loại mà không kiểm `"No known vulnerabilities found"`.
Hôm nay vô hại (có dòng advisory nghĩa là pip-audit đã chạy), nhưng nó là nửa còn
lại của chính bản vá 08-20.

## L7 - Chú thích về wheel của `lmdb` đo bản 1.7.5/1.8.1, còn bản đang dùng là 2.3.0

`pyproject.toml:212` ghi: *"Đo 2026-08-20: lmdb 1.7.5 và 1.8.1 CÓ wheel `cp314t`"*.
Sàn khai là `lmdb>=1.7.5`, không có nắp trên, nên `pip` cài bản mới nhất - đo trên
máy này: **`lmdb 2.3.0`**, tức đã **qua một bản MAJOR** so với thứ được đo.

Bước 1b thì ghim `lmdb==1.7.5` để soi advisory, nên **không phép đo nào chạm tới
2.x** ngoài việc 2412 test tình cờ chạy trên nó và xanh.

Không phải lỗi (chính sách `>=` là có chủ đích, và CLAUDE.md đã khai *"sàn là `>=`
nên pip cài bản mới nhất"*), nhưng chú thích đang mô tả một phép đo **không còn nói
về thứ người dùng nhận**. Sửa bằng một dòng: đo lại wheel trên bản hiện hành, hoặc
ghi rõ phép đo đó gắn với sàn chứ không gắn với bản được cài.

---

# ✅ Đã đo, SẠCH (đợt 3)

| Phép đo | Kết quả |
|---|---|
| **Bước 1b - `check_dep_advisories.py`** (lần đầu chạy cho 0.8) | **1 advisory**: `apscheduler 4.0.0a6 PYSEC-2026-282`, nằm trong danh sách chấp nhận kèm lý do đã viết. **Không mục nào ngoài danh sách.** Mã thoát 0 |
| **Nội dung sdist** | 289 mục, 677 KB. **Không rò** `.claude/`, `tests_temp/`, `pypi_token`, `__pycache__`, `application.yml`. Thư mục mức 1: `docs`, `xime`, `LICENSE`, `CHANGELOG.md`, `README*.md`, `pyproject.toml`, `.gitignore` |
| **Nội dung wheel** | 243 mục, 560 KB, không rò gì, `LICENSE` có mặt ở `dist-info/licenses/` |
| **`twine check`** | **PASSED** cả sdist lẫn wheel |
| **Metadata** | `Metadata-Version: 2.5` · `Version: 0.8.0` · `License-Expression: MIT` · `Requires-Python: >=3.12` · 14 extra gồm `lmdb` |
| **Giấy phép `lmdb`** | **OLDAP-2.8** (OpenLDAP Public License) so với **MIT** của xime. Permissive, tương thích, và nó là **extra tuỳ chọn** nên người cài `pip install xime` không kéo nó về. Không có việc phải làm |
| ⭐ **Đầu-cuối trên venv TRẮNG** | cài từ `xime-0.8.0.tar.gz[web]` -> `xime.__version__` = **0.8.0** -> `xime init thu-that` -> `pip install -e .` -> `python main.py` -> `GET /ping` -> **200 `{"status":"ok"}`** |
| **`ruff check xime/`** | **All checks passed** |
| **Ba script tài liệu** | `check_doc_imports` · `check_doc_register` · `check_doc_code` - cả ba **mã thoát 0** |
| **Dấu gạch dài** | **0** ở `xime/**/*.py`, `docs/**/*.md`, `README*`, `CHANGELOG.md`, `pyproject.toml`, `tests_temp/**/*.py` |

⚠ **Một cổng KHÔNG chạy được**: `mypy --strict` phía người dùng - xem T11. Đây là
mục duy nhất của đợt 3 ở trạng thái **CHƯA KẾT LUẬN ĐƯỢC**, không phải `ĐẠT`.
